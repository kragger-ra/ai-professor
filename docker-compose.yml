version: '3.8'
services:
  tts-server:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "5002:5002"
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]