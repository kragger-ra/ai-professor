# Tutor v2

Переписанный ИИ-тьютор: **один процесс, линейный голосовой конвейер**.
Заменяет легаси-архитектуру на 4 процесса + `multiprocessing.Manager`,
унаследованную от стримингового агента NetTyan.

## Конвейер

```
capture/STT  --input_q-->  agent  --tts_q-->  playback
     |                                            ^
     +------------------ interrupt ---------------+
```

Один процесс, рабочие потоки, обычные `queue.Queue`, один `threading.Event`
прерывания. Ни Manager, ни IPC, ни прокси.

## План по фазам

| Фаза | Содержание | Статус |
|---|---|---|
| 1 | Спина: потоки, очереди, прерывание, push-to-talk гейт (заглушки) | ✅ готово |
| 2 | QA-база: STT, RAG, мета пред-полётом, промпт, стрим LLM, Vosk-TTS, объект «Ответ» | ✅ готово |
| 3 | Перебивание + возврат (стек, генерация/озвучка расцеплены) | ✅ готово |
| 4 | Вложенность: стек 3 уровня, парковка отложенных, авто-предложение вернуться | ✅ готово |
| 5 | Адаптивный слой: регистр речи, чекер мини-лекций, тюнинг длины, роутер команд | ✅ готово |
| 6 | Кросс-сессионная память ✅, профиль ✅, кнопка очистки ✅, тихий эмбиент ✅. Gradio-панель и FX-звуки — отменены | ✅ готово |
| 7 | Зачистка: удалить легаси `src/` | ✅ готово |
| 8 | Аудио-роутер: замена VoiceMeeter Banana своим кодом + 1 лёгкий VB-CABLE | — |

## Запуск

```
start_tutor_v2.bat             # поднять Vosk-TTS и запустить конвейер
python -m tutor.app            # запуск напрямую (микрофон -> RAG -> LLM -> Vosk)
python -m tutor._smoke_phase2  # автотест мозга: вопрос -> ответ (нужны LM Studio + OpenAI)
python -m tutor._smoke_phase3  # автотест перебивания + возврата
python -m tutor._smoke_phase4  # автотест лимита вложенности (3 уровня)
python -m tutor.brain.answer   # автотест объекта «Ответ» и стека
```

`tutor.app` требует запущенные LM Studio (эмбеддинги RAG) и Vosk-TTS сервер,
плюс доступ к OpenAI API (или локальной модели).
